{
  "description": "Wake the session when a delegated task finishes, and clean up after workers that did not survive.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/delegate\" reconcile --project-root \"${CLAUDE_PROJECT_DIR:-$PWD}\"",
            "timeout": 15
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/delegate\" _hook-watch",
            "timeout": 3900,
            "async": true,
            "asyncRewake": true,
            "rewakeMessage": "A delegated task finished. Use the task_id and project_root in the notification to collect it exactly once, weigh the evidence, and carry on with what the user asked for.",
            "rewakeSummary": "Delegated task finished"
          }
        ]
      }
    ],
    "PostToolUseFailure": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/delegate\" _hook-watch",
            "timeout": 3900,
            "async": true,
            "asyncRewake": true,
            "rewakeMessage": "A delegated task finished. Use the task_id and project_root in the notification to collect it exactly once, weigh the evidence, and carry on with what the user asked for.",
            "rewakeSummary": "Delegated task finished"
          }
        ]
      }
    ]
  }
}
